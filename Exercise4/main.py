import timeit

def sortInt(int size, int range, bool random):

    if (random):
        numbers = random.sample(xrange(range), size)
    else:
        for[]

    for (int i = 0; i < 20; i++):
        start_time = timeit.default_timer()
        sorted(numbers)
        run_times.append(timeit.default_timer() - start_time)

    return max(run_times)

if __name__ == "__main__":
